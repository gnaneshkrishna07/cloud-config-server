server:
  port: 9090

spring:
  cloud:
    gateway:
      routes:
        - id: order-service
          uri: lb://ORDERSERVICE
          predicates:
            - Path=/orders/**

        - id: payment-service
          uri: lb://PAYMENTSERVICE
          predicates:
            - Path=/payment/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true
